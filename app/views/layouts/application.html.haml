!!!
%html
  %head
    %title Connections
    - %w{ application 960 layout }.each do |stylesheet|
      = stylesheet_link_tag stylesheet
    = javascript_include_tag "application"
    = csrf_meta_tags
    %meta{ :name => 'description', :content => ""  }
    %meta{ :name => 'keywords', :content => ""  }
  %body
    .container_16
      #header.grid_11
        %h1
          Header
      #user.grid_5
        - if user_signed_in?
          Signed in as
          = "#{current_user.email}. Not you?"
          = link_to "Sign out", destroy_user_session_path
        - else
          = link_to "Sign up", new_user_registration_path
          or
          = link_to "Sign in", new_user_session_path
      .clear
      #flash.grid_16
        .notice= notice
        .alert= alert
      .clear
      #content.grid_16
        #main.grid_11
          = yield
        #sidebar.grid_4
          = render 'sidebar'
        .clear
      .clear
      #footer.grid_16
        Footer
      .clear
    .clear


